import { lazy, useCallback, useEffect, useMemo, useState } from 'react';
import { Logo } from './components/Logo';
import { TEMPLATES } from './features/gallery/templates';
import { EMPTY_DRAFT, type Artwork, type DecorId, type GalleryDraft, type TemplateId, type WallId } from './features/gallery/types';
import { galleryRepository, type GalleryRecord } from './services/galleryRepository';

const GalleryScene = lazy(() => import('./features/gallery/GalleryScene').then((module) => ({ default: module.GalleryScene })));
const DannyDemoScene = lazy(() => import('./features/gallery/GalleryScene').then((module) => ({ default: module.DannyDemoScene })));

type Route = { page: 'home' | 'create' | 'demo' | 'gallery'; id?: string };
const routeFromHash = (): Route => {
  const hash = location.hash.replace(/^#/, '');
  if (hash === '/create') return { page: 'create' };
  if (hash === '/demo') return { page: 'demo' };
  if (hash.startsWith('/g/')) return { page: 'gallery', id: hash.slice(3) };
  return { page: 'home' };
};
const navigate = (path: string) => { location.hash = path; window.scrollTo(0, 0); };

function Header({ light = false }: { light?: boolean }) {
  return <header className={`site-header ${light ? 'site-header--light' : ''}`}><Logo dark={light} /><nav><button onClick={() => navigate('/demo')}>Live demo</button><button onClick={() => navigate('/create')}>Create gallery <span>↗</span></button></nav></header>;
}

function RoomIllustration() {
  return <div className="room-illustration" aria-hidden="true"><div className="room-ceiling"/><div className="room-wall room-wall--left"/><div className="room-wall room-wall--back"><i/><i/><i/></div><div className="room-wall room-wall--right"/><div className="room-floor"/><div className="artwork artwork--a"/><div className="artwork artwork--b"/><div className="artwork artwork--c"/><div className="plinth"/></div>;
}

function Landing() {
  return <main className="landing"><Header /><section className="hero"><div className="hero-copy"><p className="eyebrow">Virtual exhibitions, beautifully simple</p><h1>Your art.<br/><em>Beyond walls.</em></h1><p className="hero-intro">Create immersive, shareable 3D galleries in minutes. No code. No downloads. Just your work, in a space it deserves.</p><div className="hero-actions"><button className="button button--light" onClick={() => navigate('/create')}>Create your gallery <span>↗</span></button><button className="text-link" onClick={() => navigate('/demo')}>Explore a live gallery <span>→</span></button></div><div className="hero-note"><span>01</span><p>Built for artists<br/>Designed for everyone</p></div></div><RoomIllustration /><p className="vertical-word">AURA / VIRTUAL SPACE</p></section>
  <section className="promise"><p className="eyebrow">One idea, beautifully realized</p><div><h2>From studio to<br/><em>space</em> in minutes.</h2><p>A focused set of considered tools gives you everything you need to compose, customize, and publish an exhibition—without learning 3D software.</p></div><div className="steps">{['Choose a space','Add your work','Set the atmosphere','Share it'].map((step, i) => <article key={step}><span>0{i+1}</span><h3>{step}</h3><p>{['Start with one of three architect-designed gallery blueprints.','Upload images and place each piece on the walls.','Choose from a curated palette of finishes, light, and objects.','Publish instantly and invite anyone with a link.'][i]}</p></article>)}</div></section>
  <section className="demo-tease"><div><p className="eyebrow">Featured exhibition</p><h2>Danny Hirsch<br/><em>Threshold</em></h2><p>A working gallery made with AURA, adapted from Danny Hirsch Arts’ original Blender space.</p><button className="button button--light" onClick={() => navigate('/demo')}>Enter the gallery <span>→</span></button></div><button className="demo-image" onClick={() => navigate('/demo')} aria-label="Open Danny Hirsch demo"><img src="./assets/demo/danny-cover.png" alt="Dark contemporary gallery showing abstract art"/><span>Explore in 3D ↗</span></button></section>
  <section className="closing"><p className="eyebrow">Your next exhibition starts here</p><h2>Make space<br/><em>for your art.</em></h2><button className="button button--dark" onClick={() => navigate('/create')}>Create a gallery <span>↗</span></button></section><Footer /></main>;
}

function Footer() { return <footer><Logo /><p>Virtual galleries for independent artists.</p><span>© 2026 AURA</span></footer>; }

function TemplatePicker({ onChoose }: { onChoose: (id: TemplateId) => void }) {
  return <main className="picker"><Header light/><div className="picker-heading"><p className="eyebrow">Create gallery · Step 1 of 3</p><h1>Choose your <em>space.</em></h1><p>Three considered architectures. Each is fully customizable, beautifully lit, and ready for your work.</p></div><div className="template-grid">{TEMPLATES.map((template) => <button className={`template-card template-card--${template.id}`} key={template.id} onClick={() => onChoose(template.id)}><span className="template-number">{template.index}</span><div className="template-preview"><div className="mini-room"><i/><i/><i/></div><span>Use this space ↗</span></div><p>{template.label}</p><h2>{template.name}</h2><small>{template.description}</small></button>)}</div><p className="picker-footnote">All spaces are optimized for desktop, tablet, and mobile.</p></main>;
}

async function imageFromFile(file: File): Promise<Pick<Artwork, 'src' | 'aspect'>> {
  const url = URL.createObjectURL(file); const image = new Image(); image.src = url; await image.decode();
  const max = 1400; const scale = Math.min(1, max / Math.max(image.width, image.height)); const canvas = document.createElement('canvas'); canvas.width = Math.round(image.width * scale); canvas.height = Math.round(image.height * scale); canvas.getContext('2d')!.drawImage(image, 0, 0, canvas.width, canvas.height); URL.revokeObjectURL(url);
  return { src: canvas.toDataURL('image/jpeg', .82), aspect: canvas.width / canvas.height };
}

function Studio({ initialTemplate }: { initialTemplate: TemplateId }) {
  const [draft, setDraft] = useState<GalleryDraft>({ ...EMPTY_DRAFT, templateId: initialTemplate }); const [selectedId, setSelectedId] = useState<string>(); const [published, setPublished] = useState<GalleryRecord>(); const [publishing, setPublishing] = useState(false);
  const selected = draft.artworks.find((item) => item.id === selectedId); const selectArtwork = useCallback((id: string) => setSelectedId(id), []);
  const update = <K extends keyof GalleryDraft>(key: K, value: GalleryDraft[K]) => setDraft((current) => ({ ...current, [key]: value }));
  const updateArtwork = (patch: Partial<Artwork>) => setDraft((current) => ({ ...current, artworks: current.artworks.map((item) => item.id === selectedId ? { ...item, ...patch } : item) }));
  const upload = async (files: FileList | null) => { if (!files) return; const remaining = Math.max(0, 8 - draft.artworks.length); const next: Artwork[] = []; for (const file of Array.from(files).filter((item) => item.type.startsWith('image/')).slice(0, remaining)) { const image = await imageFromFile(file); next.push({ id: crypto.randomUUID(), title: file.name.replace(/\.[^.]+$/, ''), ...image, wall: 'north', x: (next.length - 1) * 1.7, y: 2.2, scale: .9 }); } setDraft((current) => ({ ...current, artworks: [...current.artworks, ...next] })); if (next[0]) setSelectedId(next[0].id); };
  const publish = async () => { setPublishing(true); try { setPublished(await galleryRepository.publish(draft)); } catch { alert('This demo stores galleries in your browser. Try fewer or smaller images.'); } finally { setPublishing(false); } };
  if (published) { const url = `${location.href.split('#')[0]}#/g/${published.id}`; return <main className="publish-success"><div><Logo/><p className="eyebrow">Gallery published</p><h1>Your space is<br/><em>ready to share.</em></h1><p>Anyone with this link can enter your exhibition.</p><div className="share-field"><input readOnly value={url}/><button onClick={() => navigator.clipboard.writeText(url)}>Copy link</button></div><div className="success-actions"><button className="button button--light" onClick={() => navigate(`/g/${published.id}`)}>Open gallery ↗</button><button className="text-link" onClick={() => { setPublished(undefined); }}>Back to editor</button></div></div><GalleryScene draft={published} visitor/></main>; }
  return <main className="studio"><header className="studio-header"><Logo/><div className="studio-title"><input aria-label="Gallery title" value={draft.title} onChange={(e) => update('title', e.target.value)}/><span>by</span><input aria-label="Artist name" value={draft.artist} onChange={(e) => update('artist', e.target.value)}/></div><button className="publish-button" onClick={publish} disabled={publishing}>{publishing ? 'Publishing…' : 'Publish'} <span>↗</span></button></header><div className="studio-body"><aside className="tool-panel"><section><p className="tool-label">01 · Artwork</p><label className="upload"><input type="file" accept="image/*" multiple onChange={(e) => upload(e.target.files)}/><span>＋</span><strong>Upload artwork</strong><small>JPG, PNG or WebP · up to 8</small></label><div className="artwork-list">{draft.artworks.map((artwork, i) => <button key={artwork.id} className={selectedId === artwork.id ? 'active' : ''} onClick={() => setSelectedId(artwork.id)}><img src={artwork.src} alt=""/><span>{String(i+1).padStart(2,'0')} · {artwork.title}</span></button>)}</div>{selected && <div className="placement"><label>Wall<select value={selected.wall} onChange={(e) => updateArtwork({ wall: e.target.value as WallId })}><option value="north">Back wall</option><option value="west">Left wall</option><option value="east">Right wall</option></select></label><label>Horizontal<input type="range" min="-3.5" max="3.5" step=".1" value={selected.x} onChange={(e) => updateArtwork({ x: +e.target.value })}/></label><label>Height<input type="range" min="1" max="3.6" step=".1" value={selected.y} onChange={(e) => updateArtwork({ y: +e.target.value })}/></label><label>Size<input type="range" min=".45" max="1.65" step=".05" value={selected.scale} onChange={(e) => updateArtwork({ scale: +e.target.value })}/></label><button className="remove" onClick={() => { setDraft((current) => ({ ...current, artworks: current.artworks.filter((item) => item.id !== selectedId) })); setSelectedId(undefined); }}>Remove artwork</button></div>}</section><Accordion title="02 · Walls"><Swatches options={[['chalk','#e7e4dc'],['warm','#b9a993'],['charcoal','#30312f']]} value={draft.wall} onChange={(value) => update('wall', value as GalleryDraft['wall'])}/></Accordion><Accordion title="03 · Floor"><Swatches options={[['concrete','#777672'],['oak','#5c4633'],['terrazzo','#a7a299']]} value={draft.floor} onChange={(value) => update('floor', value as GalleryDraft['floor'])}/></Accordion><Accordion title="04 · Lighting"><Choice options={['daylight','museum','evening']} value={draft.lighting} onChange={(value) => update('lighting', value as GalleryDraft['lighting'])}/></Accordion><Accordion title="05 · Objects"><div className="object-grid">{(['olive','monstera','arc-lamp','pedestal'] as DecorId[]).map((item) => <button key={item} className={draft.decor.includes(item) ? 'active' : ''} onClick={() => update('decor', draft.decor.includes(item) ? draft.decor.filter((x) => x !== item) : [...draft.decor, item])}>{item.replace('-', ' ')}</button>)}</div></Accordion></aside><section className="canvas-wrap"><GalleryScene draft={draft} selectedId={selectedId} onSelect={selectArtwork}/><div className="canvas-badge"><span>Editing</span>{TEMPLATES.find((item) => item.id === draft.templateId)?.name}</div></section></div></main>;
}

function Accordion({ title, children }: { title: string; children: React.ReactNode }) { return <details><summary>{title}<span>＋</span></summary><div className="detail-content">{children}</div></details>; }
function Swatches({ options, value, onChange }: { options: [string,string][]; value: string; onChange: (value: string) => void }) { return <div className="swatches">{options.map(([name,color]) => <button key={name} className={value === name ? 'active' : ''} onClick={() => onChange(name)}><i style={{ background: color }}/><span>{name}</span></button>)}</div>; }
function Choice({ options, value, onChange }: { options: string[]; value: string; onChange: (value: string) => void }) { return <div className="choices">{options.map((item) => <button key={item} className={value === item ? 'active' : ''} onClick={() => onChange(item)}>{item}</button>)}</div>; }

function Demo() { return <main className="viewer"><header className="viewer-header"><Logo/><div><p>Danny Hirsch Arts</p><span>Threshold · 2026</span></div><button onClick={() => navigate('/create')}>Create your own ↗</button></header><DannyDemoScene/><div className="viewer-caption"><p className="eyebrow">Public demo gallery</p><h1>Threshold</h1><p>Material, movement, and atmosphere by Danny Hirsch.</p></div></main>; }

function PublishedGallery({ id }: { id: string }) { const [gallery, setGallery] = useState<GalleryRecord | null | undefined>(); useEffect(() => { galleryRepository.find(id).then(setGallery); }, [id]); if (gallery === undefined) return <div className="loading">Loading space…</div>; if (!gallery) return <main className="not-found"><Logo/><h1>This gallery isn't available.</h1><p>Demo galleries currently live in the browser where they were created.</p><button className="button button--light" onClick={() => navigate('/create')}>Create a gallery</button></main>; return <main className="viewer"><header className="viewer-header"><Logo/><div><p>{gallery.title}</p><span>{gallery.artist}</span></div><button onClick={() => navigate('/create')}>Create your own ↗</button></header><GalleryScene draft={gallery} visitor/><div className="viewer-caption"><p className="eyebrow">Virtual exhibition</p><h1>{gallery.title}</h1><p>by {gallery.artist}</p></div></main>; }

export default function App() { const [route, setRoute] = useState(routeFromHash); const [template, setTemplate] = useState<TemplateId>(); useEffect(() => { const handler = () => { setRoute(routeFromHash()); setTemplate(undefined); }; addEventListener('hashchange', handler); return () => removeEventListener('hashchange', handler); }, []); const content = useMemo(() => { if (route.page === 'create') return template ? <Studio initialTemplate={template}/> : <TemplatePicker onChoose={setTemplate}/>; if (route.page === 'demo') return <Demo/>; if (route.page === 'gallery' && route.id) return <PublishedGallery id={route.id}/>; return <Landing/>; }, [route, template]); return content; }
